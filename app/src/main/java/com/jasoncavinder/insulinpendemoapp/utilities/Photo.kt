/*
 * Copyright (c) 2019, Jason Cavinder <jason.cavinder@gmail.com>.
 * This project is licenced to the client of Upwork contract #21949291. It is not
 * licensed for public use. See the LICENSE.md file for details
 */

package com.jasoncavinder.insulinpendemoapp.utilities

class Photo {

    // TODO: Image loader/saver
/*
    fun loadImage(filename: String): Bitmap {
        try {
            BitmapFactory.decodeByteArray(filename)
        } catch (e: IOException) {
            Snackbar.make()
        }
    }
    fun picture(): Bitmap? = try {
        BitmapFactory.decodeFile(providerID.replace("-", "").toLowerCase().plus(".jpg"))
    } catch (e: IOException) {
        null
    }
*/
}